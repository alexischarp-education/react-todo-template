// TODO: Create a Button component
// 1. The button receives an optional prop called variant. If variant is set to 'danger', the button becomes red.
// 2. The button should trigger the onClick callback provided as a prop when clicked.
// 3. The button should display the text provided as a prop.
